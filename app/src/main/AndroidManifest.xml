<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="kr.hs.emirim.kimjinju.gifugi">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".winter_festival_card"></activity>
        <activity android:name=".fall_festival_card" />
        <activity android:name=".summer_festival_card" />
        <activity android:name=".winter_list" />
        <activity android:name=".fall_list" />
        <activity android:name=".summer_list" />
        <activity android:name=".spring_festival_card" />
        <activity android:name=".spring_list">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>